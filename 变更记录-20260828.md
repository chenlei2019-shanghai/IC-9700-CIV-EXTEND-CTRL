# 项目变更记录（2026-08-28）

> 本文档总结 2026-08-28 对 IC-9700 CI-V 控制器项目的全部修改，供后续维护参考。

---

## 一、修复：USB AF/IF 输出电平"调小后无法调大"（核心 Bug）

### 根因

Icom CI-V 协议中，所有标注数据范围 **"0000 ~ 0255"** 的项目必须使用 **2 字节 BCD 编码**（例如 255 编码为 `0x02 0x55`，200 编码为 `0x02 0x00`）。原代码按**原始二进制**发送/解析：

- `app.py` 的 `set_1a_05`：对非 `BCD_ITEMS` 白名单的条目，值 ≤255 时只发 1 字节原始值 `bytes([val])`
- 当数值的十六进制含 A~F 半字节（如 127→`0x7F`、255→`0xFF`）时，电台判定为非法 BCD，返回 **NG 拒绝** → 表现为"调小后无法调大"
- 即使被接受也会解读成错误值（如 150→`0x96` 被当成 BCD 96）

### 修改内容

| 文件 | 修改 |
|---|---|
| `app.py` | `BCD_ITEMS` 白名单从 5 项扩充到 19 项，覆盖 UI 用到的全部 "0000~0255" 条目（ACC/USB AF/IF 输出电平、MOD 电平、Beep 电平/音调、语音电平、REF 调整、EMR AF、LCD 背光、语音TX电平、侧音电平、NB 电平/宽度、TX 功率限制值） |
| `app.py` | 0x14 电平命令的**返回值解码**从二进制 `(b1<<8)\|b2` 改为 `bcd2_to_int()`（此前电台实际值 128 会错显示为 296→116%，即"最大值显示116"的原因） |
| `civ.py` | `set_level()`（0x14 命令：AF音量、RF功率等主面板滑条）发送时改为 BCD 编码，并做 0-255 范围钳制 |
| `static/app.js` | 滑条数值显示从"百分比换算"改为**直接显示 0-255 原始值**；修复 `updateExtControl` 中 `value.length > 0` 判断导致整数回报值无法同步到滑条的问题 |

### 关键参考

- 2 字节 BCD 编码：高字节 = 千位/百位，低字节 = 十位/个位，如 255 → `0x02 0x55`
- 编码函数：`app.py` 的 `int_to_bcd2()` / 解码 `bcd2_to_int()`；`civ.py` 的 `set_level()` 内联实现

---

## 二、新增：IC-705 支持

参考文档：`ICOM IC-705 CI-V指令参考(1).pdf`（已提取为 `ic705_civ_ref.txt`）

### 两机关键差异

| 项目 | IC-9700 | IC-705 |
|---|---|---|
| 默认 CI-V 地址 | `0xA2` | `0xA4` |
| 1A 05 条目编号 | 例：USB AF 电平 = 0106 | **完全不同**，例：USB AF 电平 = 0110 |
| 模式 | 无 WFM | 有 WFM（0x06） |
| LAN 控制 | 支持（UDP 50001/50002） | **不支持**（仅 USB 串口） |
| 独有功能 | 卫星模式、Sub Band、1200M、ACC、TX功率限制 | WLAN、电池、AH-705 天调 |

### 实现方式

1. **`item_map_9700_705.py`（新增文件）**
   - 从两份官方 CI-V 文档逐项对比生成的 **IC-9700 → IC-705 条目映射表**，共 261 个两机共有条目
   - ⚠️ **注意**：文件中条目号是**十进制存储的印刷编号**（如印刷 "0106" 存为 106），`app.py` 导入时用 `_itemno()` 转换为程序内部使用的 hex 风格值（0x0106 = 262）

2. **`civ.py`**
   - 新增 `set_radio_addr()`，运行时切换电台地址；`build_command`/`parse_response` 读取模块全局 `RADIO_ADDR`，自动生效
   - `MODES` 增加 `0x06: "WFM"`

3. **`app.py`**
   - 新增机型状态 `current_model` 与 `set_rig_model()`（连接时通过 `model` 参数传入）
   - `to_radio_item()` / `to_ui_item()`：发送时把 UI（9700）编号翻译成机台编号；电台回报时翻译回 UI 编号，前端无感知
   - `is_bcd_item()`：BCD 判断按**机台侧**编号（705 用 `IC705_BCD_ITEMS_FULL`，含 0089 REF Adjust）
   - 特殊值转换：SPEECH 语言（9700: 0050，00=英语/01=日语；705: 0053，**0/1 含义相反**）自动交换
   - IC-705 请求 LAN 连接时返回错误提示
   - 已知不处理：DATA MOD 选项值列表两机不同（ACC/LAN vs USB/WLAN），未做值转换

4. **前端（`static/index.html` + `static/app.js`）**
   - 顶栏新增机型选择下拉框（IC-9700 / IC-705），连接时携带 `model` 参数
   - 选 IC-705 时：隐藏"卫星操作"面板、禁用 LAN 选项并强制串口、显示 WFM 模式按钮（通过 `data-rigs` 属性控制）
   - 连接响应消息中携带 `model`，前端据此前换 UI

### IC-705 使用注意

- IC-9700 有有线网口（LAN）；IC-705 无网口但通过 **WLAN（WiFi）支持同一 Icom UDP 协议**（wfview 兼容，已实测可用），两种机型都可选 LAN/WLAN 连接
- 0x14 电平、0x15 仪表、大部分 0x16 功能命令两机完全通用
- IC-9700 独有条目在 IC-705 上会被拒绝（NG），属正常现象

---

## 三、其他修改

- `static/index.html` 主页面新增**"关于"卡片**：署名作者 BH4FUO（MIT License），列出引用文档（IC-9700/IC-705 CI-V Reference、操作手册、Hamlib、gp2hmlb、wfview、GPredict Wiki）
- `app.js` 引用版本号更新为 `?v=20260828`（防浏览器缓存）

---

## 四、打包产物（dist/）

| 文件 | 内容 |
|---|---|
| `ic9700-ctrl-20260828-083156.exe` | 仅修复 USB AF/IF 电平 bug |
| `ic9700-ctrl-20260828-083705.exe` | + 滑条显示 0-255、0x14 编解码修复 |
| `ic9700-ctrl-20260828-083944.exe` | + 关于页面 |
| `ic9700-ctrl-20260828-090941.exe` | + IC-705 支持（最新） |
| `ic9700-ctrl.exe` | 最新构建副本 |

重新打包命令：`pyinstaller --clean --noconfirm ic9700-ctrl.spec`

---

## 五、遗留事项 / 后续可改进

1. `IC9700-CIV-CTRL-BH4FUO\` 子目录是原项目备份副本，**未同步修复**（如不再使用可删除）
2. IC-705 的 RX Position Display 等个别条目选项数量比 9700 少，多出选项会被 NG（不影响使用）
3. 705 独有功能（WFM 音调、Max TX Power、WLAN 设置、AH-705 天调等）UI 未覆盖

---

## 六、追加修复（2026-08-28 第二批）

1. **反向映射一对多**：`IC705_TO_IC9700` 改为 `{705条目: [9700条目列表]}`，新增 `to_ui_items()`；0x1A 05 回报处理时对列表中每个 UI 条目各广播一次 → IC-705 上 NB 电平/宽度/深度等共享条目能同时更新 144M 和 430M 两个滑条
2. **选项列表按机型切换**：`static/app.js` 新增 `RIG_SELECT_OPTIONS` 表，在 `applyRigModel()` 中重建下拉框选项——DATA OFF MOD / DATA MOD（9700: MIC/ACC/USB/LAN 系 vs 705: MIC/USB/WLAN 系）、GPS Select（9700 外接GPS vs 705 内置GPS）
3. **REF Adjust 滑条范围**：新增 `RIG_SLIDER_MAX` 表，IC-705 时 0072 滑条上限从 255 调整为 511（705 的 0089 粗细合一，范围 0000~0511，BCD 编码本身支持到 9999，无需改后端）
