# IC-9700 CI-V 控制器 — 项目简报

> 生成日期：2026-08-28 · 作者：BH4FUO · 许可证：MIT · 工作目录：`D:\IC9700 CIV CTRL`

---

## 一、项目一句话概述

一套面向业余无线电的 **Icom IC-9700 / IC-705 电台 CI-V 远程控制系统**，提供 Web 控制台（FastAPI + WebSocket）与 GPredict 卫星跟踪联动代理（Tkinter GUI），支持串口与 LAN/WLAN 两种连接方式，已打包为免安装的 Windows 独立 EXE。

---

## 二、项目组成（两条产品线）

### 产品线 A：Web 控制台（主线，持续迭代中）

| 文件 | 作用 |
|------|------|
| `app.py`（28.8 KB） | FastAPI 服务器 + WebSocket，主入口；含机型管理、BCD 编解码、条目映射 |
| `civ.py`（19.2 KB） | CI-V 协议编解码库：帧打包/解析、频率 BCD 转换、串口读写 |
| `lan.py`（27.4 KB） | Icom LAN/UDP 协议实现（UDP 50001 控制 / 50002 CI-V 隧道，wfview 兼容认证） |
| `item_map_9700_705.py` | IC-9700 ↔ IC-705 条目映射表（261 个共有条目） |
| `static/` | 前端：`index.html`（中文树形导航 UI）、`app.js`、`style.css`（暗色主题） |
| `dist/` | PyInstaller 打包产物，最新为 `ic9700-ctrl.exe`（2026-08-28 构建） |

**功能覆盖**：频率/模式读写、VFO 操作、记忆通道、扫描、Split/RIT、电平控制（AF/RF/SQL/PBT 等 18 项）、仪表读取（S-Meter/PO/SWR/ALC/COMP/Vd/Id）、0x16 功能开关（20+ 项）、0x1A 05 扩展设置（Beep/Connectors/Network/Display/Scope 等全部分类）、原始 CI-V 命令透传、实时通信日志。

**运行机制**：后台线程每 150 ms 轮询 8 项状态（完整周期约 1.2 s）；自动过滤电台推送的频谱数据（0x27，约 30 Hz × 500 B/帧）。

### 产品线 B：GPredict 卫星跟踪代理（独立工具，功能稳定）

| 文件 | 作用 |
|------|------|
| `gpredict_civ_gui.py` | TCP:4532 监听 GPredict rigctld 协议 → 串口直控电台，绕过 Hamlib |
| `gpredict-civ-gui.exe`（11.3 MB） | 已打包的独立 EXE |
| `gpredict_civ_proxy.py`、`gp2hmlb.py` | 早期代理版本 / DL7OAP 参考方案 |
| `sat.py` | 卫星 TLE/轨道计算 |

**解决的核心问题**：Hamlib 4.7.1 在 IC-9700 卫星模式下的 VFO 映射 Bug（Main/Sub 频率互相覆盖、跨波段命令被拒）。方案为直接 CI-V 控制 + 自动波段适配 + 上行亚音自动设置（39 种 CTCSS）。

### 辅助/资料文件

- **官方文档 PDF**：IC-9700 中文操作手册、中文高级手册、英文 CI-V 参考、IC-705 CI-V 指令参考、iGateMini 卫星手册
- **提取文本**：`pdf_text.txt`、`civ_ref_text.txt`、`ic705_civ_ref.txt`、`igatemini_text.txt`
- **临时脚本**：`fetch_*.py`、`search_*.py`（开发期爬取 Hamlib/wfview 资料用）
- `IC9700-CIV-CTRL-BH4FUO\`：**旧版备份副本，未同步最新修复**（变更记录建议可删除）
- `IC9700 CIV CTRL.zip`（80 MB）：项目压缩包

---

## 三、最新进展（2026-08-28，当日完成）

1. **修复核心 Bug**：USB AF/IF 输出电平"调小后无法调大"——根因是 "0000~0255" 范围条目必须用 2 字节 BCD 编码（255 → `0x02 0x55`），原代码按原始二进制发送被电台 NG 拒绝。BCD 白名单从 5 项扩充至 19 项，0x14 返回值解码同步修正。
2. **新增 IC-705 支持**：地址 0xA4、WFM 模式、261 项条目映射、SPEECH 语言值自动交换、反向一对多映射（NB 电平等共享条目双滑条同步）、选项列表/滑条范围按机型切换；705 通过 WLAN 使用同一 Icom UDP 协议。
3. **前端**：新增机型选择下拉框、"关于"卡片、缓存版本号 `?v=20260828`。
4. **构建**：当日产出 6 个 EXE 版本，最新为 `ic9700-ctrl-20260828-092152.exe`。
5. **Git 近期提交**：双 VFO 连续监视（MAIN/SUB 自动轮询）、卫星面板重构（CSN S.A.T 风格布局）、TLE/多普勒跟踪曾加入后简化移除。

---

## 四、技术要点速览

- **CI-V 帧**：`FE FE [To] [From] [Cmd] [Data] FD`；IC-9700 地址 0xA2，IC-705 地址 0xA4，控制器 0xE0
- **频率编码**：5 字节 little-endian BCD（例：146.520 MHz → `00 52 14 60 00`）
- **LAN 握手**：discover → IAmHere → Login(加密凭证) → Token → ConnInfo → CI-V 端口（50002）→ 保活
- **WebSocket API**：`ws://host:8080/ws`，JSON 消息（connect / set_frequency / set_level / poll / raw 等）

---

## 五、当前状态与遗留事项

**状态**：主线 Web 控制台功能完整、当日构建可用；`app.log` 显示最近一次启动为 2026-08-28 09:21，IC-9700 / IC-705 两机型均已实际连接测试。

**遗留事项**（来自变更记录）：

1. `IC9700-CIV-CTRL-BH4FUO\` 旧备份未同步修复，建议删除或归档
2. IC-705 个别条目选项数量少于 9700，多出选项会被 NG（不影响使用）
3. IC-705 独有功能（WFM 音调、Max TX Power、WLAN 设置、AH-705 天调）UI 尚未覆盖
4. DATA MOD 选项值两机不同，未做值转换
